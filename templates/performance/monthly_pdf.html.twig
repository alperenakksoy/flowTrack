<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monthly Performance Report - {{ targetUser.email }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f8f9fa;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }

        .header {
            text-align: center;
            border-bottom: 3px solid #667eea;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #667eea;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .header p {
            color: #6c757d;
            font-size: 16px;
        }

        .score-hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-align: center;
            padding: 40px;
            border-radius: 10px;
            margin-bottom: 30px;
        }

        .score-hero h2 {
            font-size: 24px;
            font-weight: 300;
            margin-bottom: 20px;
        }

        .score-hero .score {
            font-size: 72px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .score-badge {
            display: inline-block;
            background: rgba(255, 255, 255, 0.2);
            padding: 10px 30px;
            border-radius: 25px;
            font-size: 18px;
            margin-bottom: 30px;
        }

        .score-breakdown {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
            margin-top: 30px;
        }

        .score-item {
            background: rgba(255, 255, 255, 0.2);
            padding: 15px;
            border-radius: 8px;
            text-align: center;
        }

        .score-item .label {
            font-size: 12px;
            margin-bottom: 8px;
            opacity: 0.75;
        }

        .score-item .value {
            font-size: 24px;
            font-weight: bold;
        }

        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 15px;
            margin-bottom: 30px;
        }

        .metric-card {
            background: white;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 20px;
            text-align: center;
        }

        .metric-card .icon {
            font-size: 48px;
            margin-bottom: 10px;
        }

        .metric-card .label {
            font-size: 12px;
            color: #6c757d;
            margin-bottom: 10px;
        }

        .metric-card .value {
            font-size: 28px;
            font-weight: bold;
        }

        .metric-card.primary {
            background: #cfe2ff;
            border-color: #0d6efd;
        }
        .metric-card.primary .value { color: #0d6efd; }

        .metric-card.success {
            background: #d1e7dd;
            border-color: #198754;
        }
        .metric-card.success .value { color: #198754; }

        .metric-card.warning {
            background: #fff3cd;
            border-color: #ffc107;
        }
        .metric-card.warning .value { color: #cc9a06; }

        .metric-card.info {
            background: #cff4fc;
            border-color: #0dcaf0;
        }
        .metric-card.info .value { color: #087990; }

        .section {
            margin-bottom: 30px;
            page-break-inside: avoid;
        }

        .section-title {
            font-size: 24px;
            color: #212529;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #dee2e6;
        }

        .performance-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 30px;
            margin-bottom: 30px;
        }

        .performance-card {
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 25px;
        }

        .performance-card h3 {
            font-size: 20px;
            margin-bottom: 20px;
            color: #212529;
        }

        .progress-section {
            margin-bottom: 25px;
        }

        .progress-label {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 14px;
            font-weight: 600;
        }

        .progress-bar-container {
            height: 20px;
            background: #e9ecef;
            border-radius: 10px;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .progress-fill {
            height: 100%;
            transition: width 0.3s;
        }

        .progress-fill.primary { background: #0d6efd; }
        .progress-fill.success { background: #198754; }

        .stat-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 12px;
        }

        .stat-box {
            padding: 15px;
            border-radius: 6px;
            text-align: center;
        }

        .stat-box.primary { background: #cfe2ff; }
        .stat-box.success { background: #d1e7dd; }
        .stat-box.warning { background: #fff3cd; }
        .stat-box.info { background: #cff4fc; }

        .stat-box .label {
            font-size: 11px;
            color: #6c757d;
            margin-bottom: 5px;
        }

        .stat-box .value {
            font-size: 24px;
            font-weight: bold;
        }

        .stat-box.primary .value { color: #0d6efd; }
        .stat-box.success .value { color: #198754; }
        .stat-box.warning .value { color: #cc9a06; }
        .stat-box.info .value { color: #087990; }

        .time-metrics {
            display: grid;
            grid-template-columns: 1fr;
            gap: 15px;
        }

        .time-metric {
            padding: 25px;
            border-radius: 8px;
            text-align: center;
        }

        .time-metric.info { background: #cff4fc; border: 2px solid #0dcaf0; }
        .time-metric.primary { background: #cfe2ff; border: 2px solid #0d6efd; }
        .time-metric.success { background: #d1e7dd; border: 2px solid #198754; }

        .time-metric .label {
            font-size: 12px;
            color: #6c757d;
            margin-bottom: 10px;
        }

        .time-metric .value {
            font-size: 48px;
            font-weight: bold;
            margin-bottom: 8px;
        }

        .time-metric.info .value { color: #055160; }
        .time-metric.primary .value { color: #084298; }
        .time-metric.danger .value { color: #842029; }
        .time-metric.success .value { color: #0f5132; }

        .time-metric .subtext {
            font-size: 12px;
            color: #6c757d;
        }

        .priority-section {
            page-break-inside: avoid;
        }

        .priority-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-bottom: 20px;
        }

        .priority-card {
            border: 2px solid;
            border-radius: 8px;
            padding: 20px;
            text-align: center;
        }

        .priority-card h5 {
            font-size: 16px;
            margin-bottom: 15px;
        }

        .priority-stats {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-bottom: 15px;
        }

        .priority-stat {
            padding: 10px;
            background: rgba(255, 255, 255, 0.5);
            border-radius: 5px;
        }

        .priority-stat .label {
            font-size: 11px;
            color: #6c757d;
        }

        .priority-stat .value {
            font-size: 20px;
            font-weight: bold;
        }

        .progress-bar-small {
            height: 12px;
            background: rgba(0, 0, 0, 0.1);
            border-radius: 6px;
            overflow: hidden;
            margin-bottom: 8px;
        }

        .progress-fill-small {
            height: 100%;
        }

        .priority-highlight {
            background: #f8d7da;
            border: 2px solid #dc3545;
            border-radius: 8px;
            padding: 25px;
            text-align: center;
            margin-top: 20px;
        }

        .priority-highlight h5 {
            font-size: 18px;
            margin-bottom: 15px;
            color: #842029;
        }

        .priority-highlight .score {
            font-size: 56px;
            font-weight: bold;
            color: #dc3545;
            margin-bottom: 10px;
        }

        .priority-highlight .message {
            font-size: 14px;
            color: #6c757d;
        }

        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #dee2e6;
            color: #6c757d;
            font-size: 14px;
        }

        @media print {
            body {
                padding: 0;
            }
            .container {
                box-shadow: none;
            }
            .section {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
{% set taskBasics = metrics.basicMetrics %}
{% set taskTime = metrics.timeMetrics %}
{% set taskPriority = metrics.priorityMetrics %}

<div class="container">
    <div class="header">
        <h1>Monthly Performance Report</h1>
        <p>
            {% if isOwnPerformance %}
                Your Performance - {{ "now"|date("F Y") }}
            {% else %}
                {{ targetUser.email }}'s Performance - {{ "now"|date("F Y") }}
            {% endif %}
        </p>
    </div>

    <div class="score-hero">
        <h2>Monthly Performance Score</h2>
        <div class="score">{{ score|number_format(1) }}%</div>

        {% set scoreLevel = score %}
        <div class="score-badge">
            {% if scoreLevel >= 90 %}
                🌟 Outstanding Month
            {% elseif scoreLevel >= 75 %}
                👍 Strong Month
            {% elseif scoreLevel >= 60 %}
                ⚠️ Developing Month
            {% else %}
                📈 Growth Opportunity
            {% endif %}
        </div>

        <div class="score-breakdown">
            <div class="score-item">
                <div class="label">Completion Rate</div>
                <div class="value">{{ taskBasics.taskCompletionRate|number_format(1) }}%</div>
            </div>
            <div class="score-item">
                <div class="label">On-Time Rate</div>
                <div class="value">{{ taskBasics.onTimeCompletionRate|number_format(1) }}%</div>
            </div>
            <div class="score-item">
                <div class="label">Total Tasks</div>
                <div class="value">{{ taskBasics.totalTasksInPeriod }}</div>
            </div>
            <div class="score-item">
                <div class="label">Completed</div>
                <div class="value">{{ taskBasics.completedTasks }}</div>
            </div>
        </div>
    </div>

    <div class="section">
        <h2 class="section-title">📊 Key Metrics Overview</h2>
        <div class="metrics-grid">
            <div class="metric-card primary">
                <div class="icon">📊</div>
                <div class="label">Total Tasks This Month</div>
                <div class="value">{{ taskBasics.totalTasksInPeriod }}</div>
            </div>
            <div class="metric-card success">
                <div class="icon">✅</div>
                <div class="label">Completed Tasks</div>
                <div class="value">{{ taskBasics.completedTasks }}</div>
            </div>
            <div class="metric-card warning">
                <div class="icon">⏳</div>
                <div class="label">Open/In Progress</div>
                <div class="value">{{ taskBasics.openTasks }}</div>
            </div>
            <div class="metric-card info">
                <div class="icon">⏰</div>
                <div class="label">On-Time Completions</div>
                <div class="value">{{ taskBasics.tasksCompletedOnTime }}</div>
            </div>
        </div>
    </div>

    <div class="section">
        <h2 class="section-title">📋 Performance Breakdown</h2>
        <div class="performance-grid">
            <div class="performance-card">
                <h3>📋 Task Performance Details</h3>

                <div class="progress-section">
                    <div class="progress-label">
                        <span>Task Completion Rate</span>
                        <span style="color: #0d6efd; font-weight: bold;">{{ taskBasics.taskCompletionRate|number_format(1) }}%</span>
                    </div>
                    <div class="progress-bar-container">
                        <div class="progress-fill primary" style="width: {{ taskBasics.taskCompletionRate }}%"></div>
                    </div>

                    <div class="progress-label">
                        <span>On-Time Completion Rate</span>
                        <span style="color: #198754; font-weight: bold;">{{ taskBasics.onTimeCompletionRate|number_format(1) }}%</span>
                    </div>
                    <div class="progress-bar-container">
                        <div class="progress-fill success" style="width: {{ taskBasics.onTimeCompletionRate }}%"></div>
                    </div>
                </div>

                <h5 style="margin-bottom: 15px; font-size: 16px;">Task Breakdown</h5>
                <div class="stat-grid">
                    <div class="stat-box primary">
                        <div class="label">Total</div>
                        <div class="value">{{ taskBasics.totalTasksInPeriod }}</div>
                    </div>
                    <div class="stat-box success">
                        <div class="label">Completed</div>
                        <div class="value">{{ taskBasics.completedTasks }}</div>
                    </div>
                    <div class="stat-box warning">
                        <div class="label">Pending</div>
                        <div class="value">{{ taskBasics.openTasks }}</div>
                    </div>
                    <div class="stat-box info">
                        <div class="label">On-Time</div>
                        <div class="value">{{ taskBasics.tasksCompletedOnTime }}</div>
                    </div>
                </div>
            </div>

            <div class="performance-card">
                <h3>⏱️ Time Performance</h3>

                <div class="time-metrics">
                    <div class="time-metric info">
                        <div class="label">On-Time Completion</div>
                        <div class="value">{{ taskBasics.onTimeCompletionRate|number_format(1) }}%</div>
                        <div class="subtext">{{ taskBasics.tasksCompletedOnTime }} of {{ taskBasics.completedTasks }} tasks completed on time</div>
                    </div>

                    {% if taskTime.averageCompletionTime is defined and taskTime.averageCompletionTime %}
                        <div class="time-metric primary">
                            <div class="label">Average Completion Time</div>
                            <div class="value">{{ taskTime.averageCompletionTime|number_format(1) }}</div>
                            <div class="subtext">hours per task</div>
                        </div>
                    {% endif %}

                    <div class="time-metric {% if taskTime.averageDelayHours is defined and taskTime.averageDelayHours %}danger{% else %}success{% endif %}">
                        <div class="label">Average Delay</div>
                        <div class="value">
                            {% if taskTime.averageDelayHours is defined and taskTime.averageDelayHours %}
                                {{ taskTime.averageDelayHours|number_format(1) }}
                            {% else %}
                                0
                            {% endif %}
                        </div>
                        <div class="subtext">
                            {% if taskTime.averageDelayHours is defined and taskTime.averageDelayHours %}
                                hours when delayed
                            {% else %}
                                No delays this month! 🎉
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% if taskPriority.successRateByPriority is defined and taskPriority.successRateByPriority|length > 0 %}
        <div class="section priority-section">
            <h2 class="section-title">🎯 Performance by Priority</h2>

            <div class="priority-grid">
                {% for priorityStat in taskPriority.successRateByPriority %}
                    <div class="priority-card {% if priorityStat.priority == 1 %}high{% elseif priorityStat.priority == 2 %}medium{% else %}low{% endif %}">
                        <h5>
                            {% if priorityStat.priority == 1 %}
                                🔴 High Priority
                            {% elseif priorityStat.priority == 2 %}
                                🟡 Medium Priority
                            {% else %}
                                🟢 Low Priority
                            {% endif %}
                        </h5>

                        <div class="priority-stats">
                            <div class="priority-stat">
                                <div class="label">Total</div>
                                <div class="value">{{ priorityStat.totalTasks }}</div>
                            </div>
                            <div class="priority-stat">
                                <div class="label">Completed</div>
                                <div class="value" style="color: #198754;">{{ priorityStat.completedTasks }}</div>
                            </div>
                        </div>

                        <div class="progress-bar-small">
                            <div class="progress-fill-small {% if priorityStat.priority == 1 %}high{% elseif priorityStat.priority == 2 %}medium{% else %}low{% endif %}"
                                 style="width: {{ priorityStat.successRate }}%">
                            </div>
                        </div>
                        <div style="font-size: 13px; font-weight: 600;">
                            Success Rate: {{ priorityStat.successRate|number_format(1) }}%
                        </div>
                    </div>
                {% endfor %}
            </div>

            {% if taskPriority.highPrioritySuccessRate is defined and taskPriority.highPrioritySuccessRate %}
                <div class="priority-highlight">
                    <h5>🔴 High Priority Success Rate</h5>
                    <div class="score">{{ taskPriority.highPrioritySuccessRate|number_format(1) }}%</div>
                    <p class="message">
                        {% if taskPriority.highPrioritySuccessRate >= 90 %}
                            Excellent! You're crushing high-priority tasks! 🌟
                        {% elseif taskPriority.highPrioritySuccessRate >= 75 %}
                            Great work on high-priority items! Keep it up! 👍
                        {% elseif taskPriority.highPrioritySuccessRate >= 60 %}
                            Room for improvement on urgent tasks. Stay focused! 💪
                        {% else %}
                            High-priority tasks need more attention. Let's prioritize! ⚠️
                        {% endif %}
                    </p>
                </div>
            {% endif %}
        </div>
    {% endif %}

    <div class="footer">
        <p>&copy; {{ "now"|date("Y") }} FlowTrack. Generated on {{ "now"|date("F j, Y") }}</p>
    </div>
</div>
</body>
</html>
