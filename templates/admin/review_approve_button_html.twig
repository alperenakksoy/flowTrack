{% if object.approve is not true %}
    <a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('approve', object) }}">
        <i class="fas fa-check"></i> {{'Approve'}}
    </a>
{% endif %}
